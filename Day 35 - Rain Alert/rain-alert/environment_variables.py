# import os
# in the terminal: env (to see al the env variables)

# for example, I have a variable to hide from externals
# account_sid = os.environ.get("ACCOUNT_SID")
# auth_token = os.environ.get("AUTH_TOKEN")

# in the terminal: creating an environment variable
# export ACCOUNT_SID=xxxx
# export AUTH_TOKEN=xxxx

# it's necessary to export the variables in the Python anywhere
